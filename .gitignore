# Don't include the build directories in the repository
/build
/build-*
/bazel-*
/package-build
/scan-build
/_CPack_Packages

# Don't include ctags in the repository
/tags

# Don't include IDE- or editor-specific files in the repository
*.kdev4
/.kdev4
/.vscode
/_vimrc_local.vim

# Don't include built packages
*.deb
*.rpm

# Don't include gh-pages build artifacts
/Gemfile.lock
/.bundle
/.jekyll-metadata
/_site
